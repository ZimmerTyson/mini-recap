function greetUser(name) {
    console.log(`Howdy, ${name}`)
}

greetUser("Tyson");



function printName(name) {
    console.log(name);
}

printName("Hannah");


function welcomeUser(user) {
    console.log(`Welcome on in, ${user}`);
}

welcomeUser('Sydney');

function askAge() {
    let age = prompt('How old are you?');
    console.log(`User is ${age}`);
}
//askAge();

function favoriteColor(color) {
    console.log(`My favorite color is ${color}`);
}
favoriteColor("Blue");

function byeUser(user) {
    console.log(`See you later ${user}`);
}

byeUser("tyson");


function doubleNumber(num) {
    console.log(num * 2);
}

doubleNumber(4);


function squareNumber(num) {
    console.log(num * num);
}

squareNumber(5);


function greetFullName(firstName, lastName) {
    console.log(`Hey, ${firstName} ${lastName}, hope you are doing awesome!`);
}
greetFullName('tyson', 'zimmer');

function remindTask(task) {
    console.log(`try not to forget about ${task}`);
}

remindTask('trash');

function temperature(temp) {
    console.log(`Today it is: ${temp}`);
}

temperature();